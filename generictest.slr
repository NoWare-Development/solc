struct GenericStruct<T, U, V> {
  t: T;
  u: U;
  v: V;
}

generic_func<T, U, V> :: (t: T, u: U, v: V) -> GenericStruct<T, U, V> {
  nested_generic_func<T2, U2, V2> :: () {
  }

  return {
    .t = t,
    .u = u,
    .v = v,
  };
}
